<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.momo_android">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MoMo_Android"
        android:usesCleartextTraffic="true">
        <activity android:name=".splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login.ui.LoginActivity"
            android:windowSoftInputMode="adjustResize">

        </activity>
        <activity android:name=".ui.HomeActivity">

        </activity>
        <activity android:name=".diary.ui.DiaryActivity" >

        </activity>
        <activity android:name=".diary.ui.DiaryEditDeepActivity" />
        <activity
            android:name=".ui.UploadWriteActivity"
            android:windowSoftInputMode="stateHidden|adjustPan" />
        <activity android:name=".ui.UploadSentenceActivity" />
        <activity android:name=".ui.UploadFeelingActivity" />
        <activity android:name=".ui.UploadDeepActivity" />
        <activity
            android:name=".list.ui.ListActivity"
            android:theme="@style/Theme.listTheme" />
    </application>

</manifest>